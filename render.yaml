services:
  - type: web
    name: food-detection-api
    env: python
    plan: free
    buildCommand: |
      pip install --upgrade pip
      pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: MISTRAL_API_KEY
        value: ICH2b9048GrX2WXz8JsgBunZDfgMLo0G
    disk:
      name: models
      mountPath: /opt/render/models
      sizeGB: 5